Run full project validation before shipping:

1. Run `pnpm lint` — Biome lint + format check
2. Run `pnpm build` — verify production build succeeds
3. Report any errors found
4. If all pass, confirm the project is ready to ship
